import React, { Component } from 'react';
import ReactDOM from 'react-dom';

const Buttons = (props) => {

    function buttonActions(Like, Dislike)
        let buttonLike = Like
        let buttonDislike = Dislike
        if 

    return(
        <div>
        <table className="table">
            <td><button>Like</button></td>
        </table>
        <table className="table">
            <td><button>Dislike</button></td>
        </table>
        </div>
    )
}